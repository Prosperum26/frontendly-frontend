import React from 'react';

export const StickyFooter: React.FC = () => {
  return (
    <div className="sticky-footer">
      <button className="footer-button">Refresh</button>
    </div>
  );
};

export default StickyFooter;
